using System;
using UnityEngine;
using UnityEngine.UI;

namespace Anjin.EditorUtility {
	public class ScrollviewSkewer : MonoBehaviour {

		/*private ScrollRect view;

		private void Awake()
		{
			if (view == null)
				view = GetComponent<ScrollRect>();
		}

		private void Update()
		{
			if (view == null) return;

			view.GetType().GetMethod("SetContent")
		}*/
	}
}