namespace Combat.Entities
{
	/// <summary>
	/// Mostly a runtime component for basic character like properties.
	/// </summary>
	// public class ActorView : ActorBase
	// {

	// protected virtual void Update()
	// {
	// 	if (FacingDirection.magnitude < Mathf.Epsilon) return;
	// 	transform.rotation = Quaternion.LookRotation(FacingDirection.xz().normalized, Vector3.up);
	// }
	// }
}