namespace Combat
{
	public enum CoachAnim
	{
		// States
		// ----------------------------------------
		Idle,
		IdleWeak,
		IdleVictory,

		// Temporary Anims (auto-return to last idle when after)
		// ----------------------------------------
		Attack,
		Hurt,
	}
}